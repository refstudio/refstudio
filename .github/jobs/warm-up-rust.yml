name: WarmUp Rust Environment

on:
  workflow_call:

jobs:
  warm-up-rust:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - uses: ./.github/actions/setup-rust